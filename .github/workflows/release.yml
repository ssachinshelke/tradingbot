name: Build Windows Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version label for bundle names (e.g., v1.0.2-rc1)"
        required: false
        default: "manual"
      publish_release:
        description: "Attach assets to a GitHub Release"
        required: false
        type: boolean
        default: false
      release_tag:
        description: "Existing tag name to publish to (required if publish_release=true in manual run)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows Bundle
    runs-on: windows-latest
    env:
      MT5_LOGIN: "10000001"
      MT5_PASSWORD: "ci-placeholder"
      MT5_SERVER: "MetaQuotes-Demo"
      LICENSE_PUBLIC_KEY_B64: ${{ secrets.LICENSE_PUBLIC_KEY_B64 }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Validate license public key secret
        shell: powershell
        run: |
          if ([string]::IsNullOrWhiteSpace($env:LICENSE_PUBLIC_KEY_B64)) {
            throw "Missing GitHub secret LICENSE_PUBLIC_KEY_B64"
          }

      - name: Materialize license public key file
        shell: powershell
        run: |
          Set-Content -Path "license_public_key.b64.txt" -Value $env:LICENSE_PUBLIC_KEY_B64 -NoNewline

      - name: Build release zip (Windows)
        shell: powershell
        run: |
          $version = "${{ github.event_name == 'workflow_dispatch' && github.event.inputs.version || github.ref_name }}"
          if ([string]::IsNullOrWhiteSpace($version)) { $version = "manual" }
          powershell -ExecutionPolicy Bypass -File .\scripts\build_release_zip.ps1 -Version $version

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: release/*.zip
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish_release == 'true')
    needs:
      - build-windows
    runs-on: ubuntu-latest
    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-release
          path: dist-assets/windows

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
          files: |
            dist-assets/windows/*.zip
          generate_release_notes: true
